# supabase/ — історичний архів (липень 2026)

Ці SQL-файли описують схему **окремого проєкту журналу ОСББ** (`jpwelaerkkdblpmkueid`), який існував до липня 2026. Проєкт видалено після об'єднання зі складом в один Supabase-проєкт (`vkwkyhjjjmcpmiakxohw`).

**Для нового розгортання ці файли виконувати НЕ ТРЕБА.** Актуальна й дійсна схема журналу (ті самі таблиці/RPC/тригери, перенесені в об'єднаний проєкт) — у `sklad/supabase/005_merge_osbb_journal.sql`.

Файли тут лишені для історії/аудиту — щоб було видно, як виглядала схема окремого проєкту журналу до злиття, і звідки походить логіка в `005_merge_osbb_journal.sql`.

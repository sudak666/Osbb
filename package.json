{
  "name": "osbb-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build && node scripts/copy-static-assets.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke-check.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run typecheck && npm run test:runtime && npm run test:unit && npm run smoke",
    "test:runtime": "node --check src/shell.js && node --check src/auth-session.js && node --check src/shell-state.js && node --check src/supabase-api.js && node scripts/check-js-fallback-parity.mjs"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "6.3.5"
  }
}
